#include <iostream>
#include "Card.h"
#include "BlackJack.h"

using namespace std;

int main(){
    BlackJ BJ;
    Player player;

    int player_num;
    cout << "請輸入玩家人數(2 ~ 4人)：" ;
    cin >> player_num;
    for()
    BJ.ChooseBanker(player_num);
    for(int i = 0; i < players; i++){
        
    }

    //Deck deck;
    /*
    for(int i = 0; i < 52; i++){
        deck.card[i].SetId(i);
        deck.card[i].SetNum(i);
        deck.card[i].SetSuit(i);
    }
    deck.Shuffle();
    for(int i = 0; i < 10; i++){
        deck.Deal(players);
        cout << endl;
    }
    */
    
    /*
    for(int i = 0; i < 52; i++){
        deck.card[i].PrintNum();
        deck.card[i].PrintSuit();
        deck.card[i].PrintId();
        cout << endl;
    }
    */
    /*
    int times;
    int players;
    cout << "請問想要遊玩幾局Black Jack？" << endl;
    cin >> times;
    cout << "請輸入玩家人數(2 ~ 4人)：" ;
    cin >> players;

    int ante;   //底注
    cout << "請輸入下注金額：" ;
    cin >> ante;
    */
    

    

    /*
    for(int i = 0; i < times; i++){
        cout << "第" << i << "局:" << endl;
    }
    */

    //cout << "times : " << times << " players : " << players;
}